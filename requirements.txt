# torch is installed separately in Dockerfile via CPU wheel index
# tensorflow-cpu replaces tensorflow (no CUDA = much smaller image)
tensorflow-cpu

# NLP
nltk
rake-nltk
spacy
transformers
SpeechRecognition

# Data
pandas

# Web
requests
beautifulsoup4
django
gunicorn

# Audio
PyAudio
